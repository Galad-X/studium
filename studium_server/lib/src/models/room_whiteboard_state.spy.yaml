class: RoomWhiteboardState
table: room_whiteboard_states
fields:
  roomId: int, relation(parent=study_rooms)
  version: int, default=0
  strokesJson: String
  updatedById: int, relation(parent=serverpod_user_info)
  updatedAt: DateTime
indexes:
  room_whiteboard_room_idx:
    fields: roomId
    unique: true
